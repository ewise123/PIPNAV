"""CRT effects — flicker animation via CSS class toggling.

Terminal UIs can't do real scanline overlays or transparency.
Instead, we simulate a CRT flicker by periodically toggling
between slightly different amber tones on the Screen.
"""
